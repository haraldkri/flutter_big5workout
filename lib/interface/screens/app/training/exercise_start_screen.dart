import 'package:flutter/material.dart';

class ExerciseStartScreen extends StatelessWidget {
  const ExerciseStartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      key: Key("screen-start-exercise"),
      child: Text('Start Exercise Screen'),
    );
  }
}
